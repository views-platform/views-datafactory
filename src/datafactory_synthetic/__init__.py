"""Synthetic data generation on the spatiotemporal grid.

Generates data with controlled covariance structure (spatial kernels,
temporal AR processes). Provenance: config hash + seed + generator version.
"""

__all__: list[str] = []
